--- links-2.1pre7/jsint.c.orig	Sun Sep 22 10:35:39 2002
+++ links-2.1pre7/jsint.c	Thu Dec 26 03:37:35 2002
@@ -2471,7 +2471,8 @@
 	if (!form)return;
 
 	u=get_form_url(fd->ses,fd,form,&has_onsubmit);
-	goto_url_f(fd->ses,NULL,u,NULL,fd,form->form_num, has_onsubmit,0);
+	goto_url_f(fd->ses,NULL,u,NULL,fd,form->form_num, 0 /*has_onsubmit*/,0);
+        /* disabled has_onsumbit - deferring here prevents submitting form in timeout handler! */
 	mem_free(u);
 	draw_fd(fd);
 	change_screen_status(fd->ses);
